services:
  web:
    environment:
      DATABASE_URL: "${DATABASE_URL}"
      DIRECT_URL: "${DIRECT_URL}"
      REDIS_URL: "${REDIS_URL}"
    depends_on: []

  worker:
    environment:
      DATABASE_URL: "${DATABASE_URL}"
      DIRECT_URL: "${DIRECT_URL}"
      REDIS_URL: "${REDIS_URL}"
    depends_on: []
